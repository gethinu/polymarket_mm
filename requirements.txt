# Runtime dependencies (pinned for reproducibility — this code can place live trades).
# Refresh with: python -m pip install -U py-clob-client websockets && python -m pip freeze
py-clob-client==0.34.6
websockets==16.0
